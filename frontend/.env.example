# Base URL of the standalone Express backend (note the /api suffix).
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Used by src/proxy.js (Next.js middleware) to verify the JWT cookie for route
# guarding. MUST be identical to the backend's TOKEN_SECRET.
TOKEN_SECRET=replace-with-a-long-random-secret
